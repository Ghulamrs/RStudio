*.o
*.d
*.obj
obj/
RStudio
RStudio.exe
RStudioGui.exe
test
test.exe
tests/test
tests/test.exe
tests/session
tests/session.exe
*.pdb
*.ilk
*.o.tmp
.DS_Store
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/project.xcworkspace/
DerivedData/
winforms/x64/

# Visual Studio's per-user project settings. Written the first time the window
# is opened in the IDE, and nobody else's business.
*.vcxproj.user
winforms/obj/
*.pdb
*.log
gui.png
*.png

# Claude Code personal settings - never commit, holds local paths
.claude/settings.local.json

# Xcode's own build directory, when the project is built in place
build/

# What a workspace build leaves here: the two compilers RStudio drives, built
# into this directory rather than collected into it, and shc's runtime
# archives - which live beside shc because that is where shc looks for them.
# None of the three is this repository's to carry.
cc1.exe
shc.exe
lib/

# The same build, into bin/ instead, for anyone who would rather the checkout
# stayed clean.
bin/

# A scratch file that appeared in the root and was swept into a commit by
# git add -A. It held an API key, and GitHub refused the push. Ignored so that
# it cannot happen twice.
Untitled.swift
